console.log("Vite is running!");

// Import scripts and styles
import "./sightreader.js";
import "/css/sightreader.css?url";

// Ensure DOM is loaded
document.addEventListener("DOMContentLoaded", () => {
    console.log("Page fully loaded!");
});
